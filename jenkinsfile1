@Library('jenkins-library@main')_

pipeline {
    agent any

    stages {
        stage('Parameter') {
           steps {
              deriveJobParams()
            }
          }
        stage('Hello') {
            steps {
                test()
            }
          }
        stage('Git Checkout') {
            steps {
            gitCheckout(
                branch: "main",
                url: "https://github.com/Manjunathankrishnan123/Jenkins.git"
                )
            }
         }
      
       }
}
